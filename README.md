# Buzzer
Buzzer Ionic app

## Setup
	npm install -g cordova
	npm install -g ionic
## Run in browser
	ionic serve

## Run on Android device
	export ANDROID_HOME=/my/path/to/android/sdk
	ionic build android
	ionic run android